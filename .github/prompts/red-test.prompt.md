---
mode: 'agent'
description: 'TDD: write a single failing test (red step)'
---
Write a single failing test for the next small piece of functionality: ${input:feature:describe the feature/behavior}.

- Do not write any implementation code yet — only the test.
- Use the existing test framework/conventions in this codebase.
- Run the test suite and confirm the new test fails for the expected reason (not a syntax/setup error).
- Show me the failing test output.
